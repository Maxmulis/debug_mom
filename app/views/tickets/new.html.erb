<h1>Submit your issue</h1>
<div class="container">
  <%= simple_form_for @ticket do |f| %>
  <%= f.input :description %>
  <%= f.input :photos, as: :file, input_html: { multiple: true } %>
  <%= f.button :submit %>
  <% end %>
</div>
